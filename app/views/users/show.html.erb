<h1>Profile</h1>

<%= link_to 'Home', root_path %>

<p><%= @user.name %></p>

<div>
  <% @user_knowledge_domains.each do |user_knowledge_domain| %>
    <%= render partial: 'knowledge_domain_card', locals: { user_knowledge_domain: user_knowledge_domain, profile_user: @user } %>
  <% end %>
</div>